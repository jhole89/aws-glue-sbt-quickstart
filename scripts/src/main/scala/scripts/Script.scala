package scripts

import shared.BaseGlueScript

object Script extends BaseGlueScript {
  //  Glue script goes here

  // run is the entry point invoked
  override def run(myArg: String): Unit = {
    ???
  }
}
